# Motorola IMPRES Battery Reader (ESP32) — повна інструкція

Пристрій для **зчитування, аналізу, редагування та програмування** чипів
акумуляторів Motorola IMPRES через веб-інтерфейс Wi-Fi і OLED/LCD-дисплей.

- **DS2433** — EEPROM 512 байт (модель, крива розряду, історія ємності,
  лічильники, блок автентифікації).
- **DS2438** — монітор батареї (напруга, струм, температура, лічильники
  заряду/розряду ICA/CCA/DCA, калібрування).

Інтерфейс українською, заставка з емблемою Національної Гвардії України.

> ⚠️ **1-Wire family codes для ЦЬОГО заліза — `DS2433_ID = 0xA3`,
> `DS2438_ID = 0xA6`** (перевірено осцилографом, у `battery_reader.h`). Це
> НЕ стандартні 0x23/0x26 з даташита. Заміна ламає зчитування — не чіпати.

---

## 1. Що потрібно

**Плата:** ESP32 (пакет `esp32` від Espressif, перевірено на 3.3.10).
⚠️ **Лише звичайний ESP32**: Bluetooth типово увімкнено, а на S3/C3/C6
класичного Bluetooth немає фізично (там тільки BLE). Потрібна плата з цієї
родини — закоментуйте `BT_ON` у `settings.h`.

⚠️ **Типовий пристрій — `DEVICE_BUCK`** (власний понижувач, квадратна панель
240×240). Складаєте прошивку для приладу на готовій платі TL494 з панеллю
240×280 — задайте `DEVICE_TL494` у `settings.h`: інакше екран лишиться
порожнім при піднятій підсвітці (див. CHANGES.md 3.61).

**Зв'язок із клієнтами — Wi-Fi АБО Bluetooth, за вибором у меню приладу**
(група «ЗВ'ЯЗОК», п'ятий рядок списку). Разом вони не піднімаються навмисно:
радіо в ESP32 одне, а стек Bluetooth бере ~70 КБ пам'яті одним шматком —
саме через це раніше «з увімкненим Bluetooth пристрій переставав
під'єднуватись по Wi-Fi». Вибір зберігається й застосовується при старті,
тож після перемикання прилад перезавантажується сам. У режимі Bluetooth
точки доступу й веб-інтерфейсу НЕМАЄ: прилад видно як звичайний COM-порт
(SPP), і з ним працюють ті самі клієнти — `moto_gui.py`, `client_usb.html`,
`moto_bridge.py`, — треба лише обрати інший порт. Запис по радіо вимагає
`AUTH <пароль>`; читання вільне.

**Схема розділів** (Інструменти → Partition Scheme):

> ⚠️ **Оберіть «Huge APP (3MB No OTA/1MB SPIFFS)». Це не порада, а умова
> збірки.** У типовій схемі «Default 4MB with spiffs» під програму лишається
> 1 310 720 байтів, і прошивка в них більше **не влазить** — IDE каже
> `Sketch too big; text section exceeds available space in board`.

| Конфігурація | Схема |
|---|---|
| **будь-яка** | **Huge APP (3MB No OTA/1MB SPIFFS)** — програмі 3 МБ (зайнято ~42 %), SPIFFS 1 МБ |
| ~~Default 4MB with spiffs~~ | ❌ не збирається: 1.31 МБ прошивки проти 1.25 МБ розділу |

**SPIFFS у 1 МБ вистачає з запасом:** `index.html` ~242 КБ, логотип і заставка —
разом менше 700 КБ.

⚠️ Раніше тут було написано, що типової схеми досить і прошивка займає ~80 %
флеша. Це було правдою до появи кривої заряду 2S, ручного струму, синхронізації
дзеркала й переносу тексту — тепер ні. Якщо ви орієнтувались на ту таблицю й
отримали помилку збірки — річ саме в цьому рядку, а не у вашій установці.

**Бібліотеки** (Arduino Library Manager):
- `U8g2` (oliver) — монохромні OLED/LCD
- `OneWire` — шина 1-Wire
- `ArduinoJson`
- *(лише для кольорових TFT ST7789)* `Adafruit GFX Library`,
  `Adafruit ST7735 and ST7789 Library`, `Adafruit BusIO`, `U8g2_for_Adafruit_GFX`
- *(лише для заставки у форматі JPEG)* `TJpg_Decoder` (Bodmer) — без неї
  закоментуйте `DISPLAY_SPLASH_JPEG`, лишиться сирий `.bin`

Bluetooth власної бібліотеки не потребує: `BluetoothSerial` входить у пакет
ESP32.

---

## 2. Підключення

> 📐 Уся схема одним аркушем — **[`SCHEMATIC.html`](SCHEMATIC.html)**: п'ять листів
> (структура, каскад заряду, вимірювання, 1-Wire, розряд), карта пінів і перелік
> доопрацювань. Відкривається просто у браузері, без сервера.

| Сигнал | GPIO | Константа | Примітка |
|---|---|---|---|
| 1-Wire дані (DS2433/DS2438) | **13** | `DS_PIN` | до контакту даних АКБ |
| Керування підтяжкою 1-Wire | **12** | `PULLUP_PIN` | сильний pull-up на час запису (tPROG) |
| LED зелений | **14** | `LED_GREEN_PIN` | через резистор ~330 Ом на GND |
| LED червоний | **27** | `LED_RED_PIN` | через резистор ~330 Ом на GND |
| Три кнопки на одному піні | **33** | `MENU_BTN_ADC_PIN` | аналогова драбинка, **лише ADC1** (32–39) |
| Затвор MOSFET розряду | **19** | `LOAD_PIN` | + резистор 10 кОм затвор→GND (обов'язково) |
| ШІМ заряду → **керуючий NPN** → силовий ключ ¹ | **26** | `CHARGE_PWM_PIN` | пін НЕ керує ключем напряму. Драйвер: **390 Ом** пін→база NPN (⚠️ на платі 20 кОм — замінити), **10 кОм** база NPN→GND, **120 Ом** колектор NPN→база/затвор (⚠️ на платі 1 кОм), **1 кОм** база/затвор→+. Ключ — **P-MOSFET NTD2955G** (або PNP B772M, див. `CHARGE_SWITCH_TYPE`) |
| Вимір струму заряду ¹ | **35** | `CHARGE_ISENSE_PIN` | шунт 0.5 Ом у мінусі → **1 кОм** → пін; **лише ADC1** |
| Вимір напруги заряду ¹ | **34** | `CHARGE_VSENSE_PIN` | подільник **20к/10к** → **1 кОм** → пін; **лише ADC1** |
| Контроль живлення +14 В ¹ | **39** | `CHARGE_PSU_PIN` | подільник **68к/10к** → **1 кОм** → пін; **лише ADC1**. Допуск 12.5…16 В |
| Enable плати TL494 ² | **32** | `CHARGE_TL_EN_PIN` | **LOW = безпечно.** Плата пропускає вхід на вихід (~14 В) без керування, тож це єдиний запобіжник |
| Аналогове керування напругою ² | **26** | `CHARGE_TL_CTRL_PIN` | ШІМ 20 кГц / 11 біт → RC на платі → вивід керування TL494 |

> ¹ — лише за `CHARGE_TOPOLOGY CHARGE_TOP_BUCK` (власний понижувач).
> ² — лише за `CHARGE_TOPOLOGY CHARGE_TOP_TL494` (готова плата DC/DC).
> Рядки з різних варіантів **не поєднуються**: піни одного заліза в іншому не
> існують, і прошивка не дасть себе зібрати, якщо їх переплутати.

> ⚖️ **Якщо напруга живлення показується заниженою** (типовий вигляд: блок на
> 14.3 В читається як 12.4 В і виводить «⛔ напруга занижена»), справа майже
> напевно не в подільнику, а в перерахунку відліку АЦП: пряма модель
> «0…3300 мВ» не описує АЦП ESP32 на 11 дБ, і в кожного чипа своя похибка.
> Перевірка займає хвилину: мультиметром на самому GPIO39 має бути
> ≈ `U_блока / 7.8` (для 14.4 В — близько 1.85 В). Якщо там саме стільки —
> подільник справний, і треба лише підправити `CHARGE_PSU_CAL_X1000` у
> `settings.h`: відкрийте `http://<адреса>/api/charge`, візьміть звідти
> `psuMv` у мілівольтах і порахуйте `CAL = 1000 × справжня_напруга / psuMv`.
> Заводське значення — `1153` (підібране на платі власника), `1000` вимикає
> поправку. Якщо ж на GPIO39 напруга НЕ та — шукайте номінал подільника, а не
> крутіть коефіцієнт.

> ⚠️ Резистор **1 кОм послідовно** перед КОЖНИМ вимірювальним піном (і струму, і напруги) — струмообмеження входу АЦП. На показання він **не впливає**: вхід АЦП високоомний, струму через резистор немає, отже немає й падіння на ньому. У жодну формулу перерахунку він тому й не входить — не «виправляйте» це.

> 🔊 Звук увімкнено: `BUZZER_DAC_PIN 25` у `settings.h` — п'єзо через
> розділовий конденсатор 1–10 мкФ. Заряд GPIO25 не займає (він на 26), тож із
> двох ЦАП-пінів ESP32 звуку лишається рівно один.

> ⚡ **ЯКА САМЕ СИЛОВА ЧАСТИНА — ЗАДАЄТЬСЯ В КОДІ.** Пристроїв два, і зарядне
> залізо в них різне не номіналами, а способом керувати й вимірювати:
>
> ```c
> #define CHARGE_TOPOLOGY CHARGE_TOP_BUCK    // понижувач, зібраний тут
> #define CHARGE_TOPOLOGY CHARGE_TOP_TL494   // готова плата DC/DC на TL494
> ```
>
> У понижувача регулятор крутить **шпаруватість** ШІМ, а струм і напругу
> міряють власні шунт і подільник. На платі TL494 регулятор крутить
> **вихідну напругу** (enable + аналогова уставка), а міряє за нього DS2438
> усередині пакета. Тому на платі TL494 **немає** контролю +14 В, пікової
> відсічки, свідка насичення клеми й примусового пробудження — прошивка
> оголошує їх недоступними прямим текстом, а не мовчить.
>
> Наступний абзац — про **вибір ключа всередині понижувача**; до плати TL494
> він не стосується (там ключ розпаяв виробник).
>
> ⚙️ **ЯКИЙ СИЛОВИЙ КЛЮЧ СТОЇТЬ — ЗАДАЄТЬСЯ В КОДІ.** Обв'язка в двох
> варіантів спільна, а фізика різна, тож вибір робиться однією константою в
> `settings.h`, і за нею розгалужуються всі розрахунки й перевірки:
>
> ```c
> #define CHARGE_SWITCH_TYPE CHARGE_SW_PMOS      // P-MOSFET NTD2955G — ЗАРАЗ
> // #define CHARGE_SWITCH_TYPE CHARGE_SW_BJT_PNP  // біполярний PNP B772M
> ```
>
> Поставите інший прилад — **перемкніть константу**, інакше прошивка рахуватиме
> струм бази для польового ключа (або напругу на затворі для біполярного) і
> дасть красиві неправильні числа. Тип, з яким зібрано, друкується при старті.

> 🔴 **ПЕРЕД ПЕРШИМ ЗАРЯДОМ ТРЕБА ПЕРЕПАЯТИ РЕЗИСТОР** (із біполярним ключем —
> два, і поодинці жодна заміна нічого не дає):
>
> | Де | Стоїть | Треба | Без заміни: PNP B772M | Без заміни: P-MOSFET |
> |---|---|---|---|---|
> | пін 26 → база керуючого NPN | 20 кОм | **390 Ом** 🔴 | NPN дає 0.13 мА замість 6.7 — не насичується | те саме: NPN не пропускає наскрізний струм дільника затвора (12.3 мА), затвор не притягується |
> | колектор NPN → база/затвор | 1 кОм | **120 Ом, 2 Вт** | струм бази 12 мА замість 106 — ключ не насичується | 🟡 **не смертельно**: \|VGS\| = 6.9 В при порозі 4 В, ключ відкривається, але з опором каналу вище паспортного |
>
> **Із біполярним** наслідок в обох випадках однаковий: B772M працює в
> **лінійному режимі** під 14 В і згорає за секунди.
> **Із польовим** обов'язкова лише перша заміна; друга дає нижчий RDS(on) і
> швидше перемикання, тобто менший нагрів.
>
> Прошивка з нинішніми номіналами **збереться** (номінали плати записані
> окремо, як довідка), при старті надрукує попередження з числами й скаже, яка
> заміна критична, а заряд зупинить сама відсічкою «ключ не тягне» приблизно
> за 5 секунд — але розраховувати на це як на захист не варто.

Усі піни змінюються у `settings.h`.

**1-Wire — ДВА резистори, обидва обов'язкові:**

| Куди | Номінал | Навіщо |
|---|---|---|
| лінія даних GPIO13 → +3.3 В | 2.2…4.7 кОм | чипи пакета живляться **паразитно** з цієї лінії; внутрішня підтяжка ESP32 (~45 кОм) її не замінює |
| enable GPIO12 → +3.3 В | 4.7 кОм — **не обов'язковий** | стоїть на платі, поставлений для налагодження. Прошивка веде цю лінію активно в обидва боки, тож підтяжка тут не умова роботи |

Enable — це не просто вихід керування: цією ж лінією **активується пакет**, і
рівень на ній має триматись і тоді, коли пін не веде її активно. Підтяжка робить
лінію визначеною завжди, а прошивка лише притискає її вниз, коли пакет має бути
неактивним.

> ⚠️ **GPIO12 — strapping-пін MTDI**, і підтяжка вгору на ньому при скиданні
> вибирає напругу флеш-пам'яті 1.8 В. Те, що ця плата з резистором стартує,
> означає, що eFuse напруги флеша вже пропалено (`espefuse.py set_flash_voltage
> 3.3V`) або флеш там 1.8 В. При переносі на інший модуль перше, що треба
> перевірити, — чи він завантажується.
`PULLUP_PIN` короткочасно дає сильну підтяжку під час програмування EEPROM.

---

## 3. Вибір дисплея

У `settings.h`, **КРОК 1** — розкоментуйте **рівно один** рядок:

```c
#define DISPLAY_SSD1306_I2C        // 0.96" 128x64 I2C (GME12864) — за замовч.
// #define DISPLAY_SH1106_I2C      // 1.3"  128x64 I2C
// #define DISPLAY_SH1107_128_I2C  // GME128128-02, 128x128 I2C (варіант SH1107)
// #define DISPLAY_SSD1327_128_I2C // GME128128-02, 128x128 I2C (варіант SSD1327)
// #define DISPLAY_ST7567_SPI      // Open-Smart 1.8" 128x64 SPI
// #define DISPLAY_PCD8544_SPI     // Nokia 5110 (PCD8544) 84x48 SPI
// --- КОЛЬОРОВІ TFT ST7789 (SPI): кольорове меню + заставка ---
// #define DISPLAY_ST7789_SPI
//   #define DISPLAY_ST7789_240X240 // ST7789VW 240x240
//   #define DISPLAY_ST7789_240X280 // ST7789V3 240x280
```

Роздільність, розкладка меню і шрифти підставляються автоматично
(на 128×128 — більший шрифт і більше рядків).

**Кольорові TFT ST7789** (240×240 VW / 240×280 V3): розкоментуйте
`DISPLAY_ST7789_SPI` + один розмір. **Типово увімкнено 240×240 (GMT130)**.
Піни SPI спільні з КРОК 2b (`SCK=18, MOSI=23, CS=5, DC=17, RST=16`),
підсвітка — `DISPLAY_BLK_PIN`
(необов'язково). Орієнтація — `DISPLAY_ST7789_ROT 0…3`, інверсія —
`DISPLAY_ST7789_INVERT`. Потрібні 4 бібліотеки Adafruit + `U8g2_for_Adafruit_GFX`
(див. розділ 1). Меню/заставка/індикатори — кольорові, реалізація у
`display_color.h` (монохромний шлях не зачіпається).

**КРОК 2a — I2C-дисплеї.** `SDA=21`, `SCL=22`, адреса `0x3C`. Працюють на
будь-яких GPIO.
- `#define DISPLAY_HW_I2C` — апаратний I2C (рендер у ~10× швидше).
  **Якщо апаратна шина не відповідає, прошивка САМА переходить на програмний
  I2C на тих самих пінах** — дисплей працює завжди; у Serial (115200) буде
  `HW I2C OK` або `SW I2C fallback`. Часта причина відмови HW I2C — слабкі
  підтяжки: поставте зовнішні **4.7 кОм** з SDA і SCL на 3.3V.
- `#define DISPLAY_I2C_KHZ 0` — частота шини, `0` = АВТО (безпечний максимум
  з драйвера: SSD1306/SH1106/SH1107 — 400 кГц, SSD1327 — лише 100 кГц).

**КРОК 2b — SPI-дисплеї** (ST7567 / Nokia 5110 / ST7789). Апаратний SPI ESP32:
`SCK=18`, `MOSI=23` (до CLK/DIN/SDA), керуючі `CS=5`, `DC=17`, `RST=16`.

> ⚑ **На модулях ST7789 240x240 (GMT130) виводу CS фізично немає** — на
> гребінку виходять лише `GND / VCC / SCL / SDA / RES / DC / BLK`, а GPIO5
> нікуди не підключений. Рядок `DISPLAY_CS_PIN` попри це **заданий**: смикати
> непід'єднаний пін електрично безкоштовно, а режим «CS немає» (`-1`)
> спирається на внутрішню гілку бібліотеки, реалізовану в різних версіях
> `Adafruit_SPITFT` по-різному. Заданий пін працює скрізь.
> Закоментувати рядок можна — тоді GPIO5 звільняється, але шину SPI не можна
> буде ділити ні з чим.
> Єдина умова: шина SPI має лишатись тільки під дисплеєм (без CS він слухає її
> постійно). Перевірка при збірці стежить, щоб `GPIO18/23` не зайняли чимось
> іншим.
- `DISPLAY_ST7567_XOFF` (за замовч. 4) — зсув картинки, підберіть 0…4, якщо
  зсунута.
- `#define DISPLAY_CONTRAST 128` (опційно) — ручний контраст (для ST7567
  більше = темніше, типово 30…90).

> GME128128-02 буває на SH1107 **або** SSD1327 — якщо картинка спотворена,
> перемкніть між цими двома опціями. Якщо обрано **SSD1327**, а екран пустий
> чи спотворений — панелі бувають з різним контролером-remap: у `settings.h`
> розкоментуйте один із `SSD1327_WS` / `SSD1327_EA` / `SSD1327_ZJY`
> (за замовч. MIDAS). Також допомагає задати `DISPLAY_CONTRAST` (для SSD1327
> усталено 0x7F). Активний дисплей і частоту I2C видно в Serial при старті. На Nokia 5110 (84×48) частина нижніх
> рядків не влазить — усі дані краще дивитись у вебі.

### Якщо екран лишається чорним

**Спершу — дві найчастіші причини саме на панелях без виводу CS** (як
GMT130-V1.0). Обидві тепер налаштовуються в `settings.h`, і типові значення вже
виставлені під цей модуль.

> ✅ На цьому пристрої чорний екран був саме через **режим SPI**: типові
> значення нижче вже правильні, і окремо налаштовувати нічого не треба.
> Розділ лишається для інших панелей і для випадку, якщо колись знадобиться
> знову.


| Причина | Константа | Типово | Що робити |
|---|---|---|---|
| не той **режим SPI** ← *саме це й було тут* | `DISPLAY_ST7789_SPI_MODE` | **3** для 240×240 | спробуйте `0` — і навпаки |
| завелика **частота SPI** | `DISPLAY_ST7789_SPI_HZ` | 24 МГц | знижуйте: `10000000`, потім `8000000` |

Режим важливий саме через відсутність CS: він припаяний до землі, контролер
вибраний **завжди** і межу посилки визначає за станом такту в спокої — а він у
режимів 0 і 3 різний. З «не тим» режимом панель приймає біти зі зсувом, жодної
команди не впізнає й лишається чорною, хоч усе припаяно правильно.

Частота — класика дротів-перемичок: на доріжках плати десятки МГц працюють, на
40-сантиметрових перемичках дають «сніг» або той самий чорний екран.

**Далі — розрізнити підсвітку й обмін.** Сама по собі чорнота нічого не каже:
однаково виглядають **згасла підсвітка** і **мовчазна шина SPI**. Розкоментуйте
в `settings.h`:

```c
#define DISPLAY_ST7789_SELFTEST
```

Після прошивки дисплей на 3 секунди заллється чистими кольорами при повній
підсвітці, а кроки продублюються в Serial (115200):

| Що бачите | Висновок | Куди дивитись |
|---|---|---|
| кольори змінюються | підсвітка й обмін **справні** | верстка, орієнтація `DISPLAY_ST7789_ROT`, оффсети |
| екран світлий, але кольорів немає | підсвітка є, **обміну немає** | SDA→GPIO23, SCL→GPIO18, DC→17, RES→16, живлення панелі, CS |
| екран лишається темним | **немає підсвітки** | BLK→GPIO4 і її живлення |

У журналі при старті прошивка друкує повну конфігурацію екрана:

```
DISPLAY: ST7789 240x240 (panel 240x240) color, rot=1, SPI mode=3 24.0 МГц, DC=17 RST=16 CS=5 BLK=4
```

Якщо цього рядка немає взагалі — до ініціалізації дисплея справа не дійшла,
причина раніша (живлення, збірка, паніка при старті — дивіться `RESET REASON`
у тому ж журналі).

---

## 4. Прошивка

1. Оберіть плату ESP32 і порт (*Tools → Board / Port*).
2. Скомпілюйте та завантажте скетч `motorola-battery-reader-web.ino`.
3. **Більше нічого заливати не треба.** Сторінка вшита в прошивку
   (`page_index.h`) і працює одразу. Кроку «залити SPIFFS» тут раніше не
   вистачало найчастіше: у Arduino IDE 2.x пункту *ESP32 Sketch Data Upload*
   немає зовсім, а зміна схеми розділів робить старий образ немонтовним — і
   він мовчки форматується, після чого браузер показує порожній екран.

### Як переконатись, що залилась саме нова прошивка

Прилад **сам називає дату й час своєї збірки** — їх підставляє компілятор, тож
підробитись вони не можуть. Дивіться:

| Де працюєте | Де відбиток |
|---|---|
| веб-інтерфейс | у шапці сторінки, поруч із назвою — на **будь-якій** вкладці |
| `client_usb.html` | у шапці, одразу після під'єднання |
| `MotoIMPRES.exe` | у заголовку вікна |

Якщо там стара дата — прошивка не залилась, і шукати причину в поведінці
приладу немає сенсу. Так уже двічі виходило: правку шукали в приладі, який її
не мав.

---

## 5. Перше під'єднання

1. Підключіться до Wi-Fi **`BatteryReader_moto`**, пароль **`12345678`**.
2. **Captive-portal:** телефон/ПК сам запропонує «Вхід у мережу» і відкриє
   сторінку. Якщо ні — відкрийте `http://192.168.4.1`.
3. Пароль на операції **прибрано** — запис/скидання доступні напряму (керування
   локальним доступом до пристрою).

---

## 6. Веб-інтерфейс

### Вкладка «Огляд»
Заряд (%, з джерелом ICA/напруга), напруга, температура, модель, ємність,
знос, кількість циклів заряд/розряд, справжність (OK / РИЗИК + причина).
Кнопки: **Зчитати АКБ**, **Оновити**.

### Вкладка «✍️ Редактор»
Одне місце, де видно **весь** стан пакета одразу й де кожне значення можна
виправити руками: усі лічильники, дати, наробітки, паспортна ємність, знос і
паливомір. Порожнє поле означає «не чіпати» — записуються лише змінені рядки,
і перед записом прилад показує, що саме поїде в пакет.

Список згруповано за чипом:

| DS2433 — памʼять самого пакета | DS2438 — монітор |
|---|---|
| дата виготовлення, дата першого запуску, дата останнього заряду, дата останнього кондиціювання | наробіток (доби) |
| цикли IMPRES, цикли не-IMPRES, калібрувальні цикли | лічильник заряду, лічильник розряду |
| наробіток у пакеті: заряд і розряд | залишок (паливомір ICA) |
| паспортна ємність, знос | мітка останньої події |

> ⚑ **«Мітка останньої події» — те, через що наробіток повертався після
> скидання.** У моніторі три мітки однієї й тієї ж події: дві в секундах і
> третя, за 0x32, у добах. Останню ми роками лишали недоторканою — і станція
> відновлювала з неї наробіток, бо «подія пізніша за наробіток» для неї
> неможливий стан. Тепер вона обнуляється разом із наробітком і видима тут.
> **Якщо після зарядки наробіток знову виріс — подивіться на цей рядок до й
> після: він скаже, звідки взялось число.**

### 📸 Знімок і різниця — що саме змінила станція

Якщо після зарядки числа знову виросли, не гадайте, звідки вони взялись —
**виміряйте**:

1. зчитайте АКБ;
2. **«Зняти знімок»** — прилад запам'ятає обидва чипи як є;
3. поставте пакет у зарядку, дістаньте;
4. **«Зчитати АКБ»**, потім **«Порівняти з знімком»**.

Прилад покаже **всі** змінені байти обох чипів: зсув, що це за поле, було й
стало. Знімок лежить у пам'яті приладу й переживає перезавантаження.

> ⚑ **Рядок «невідоме поле» — найцінніший у цій таблиці.** Він означає, що
> станція пише туди, куди прошивка не дивиться. Саме так знайшлися всі чотири
> місця, де пакет тримав наробіток; п'яте, якщо воно є, знайдеться так само —
> тільки за один захід, а не за три листування.

### Помилку введення прилад виправляє сам

Вам не треба вгадувати, яке число підійде: у кожної помилки тут одна очевидна
правильна відповідь, і прилад її підставляє — **називаючи, що саме виправив**.

| Ввели | Стане | Чому |
|---|---|---|
| число поза межами | найближча межа | межі показані підказкою в рядку |
| дата в майбутньому | сьогодні | пакета, який почав працювати завтра, не буває |
| 13-й місяць, 45-те число | 12-й, останнє в місяці | з урахуванням високосного року |
| нуль у даті виготовлення | та, що вже стоїть | без неї решті нема від чого рахуватись |
| запуск раніший за виготовлення | підтягується до виготовлення | дати живуть зміщенням від нього |
| «вмикали, але не заряджали» | заряд у день першого запуску | найменша правда, яка це розв'язує |

Виправлення видно **двічі**: підказкою одразу, щойно ви виходите з поля, і
вікном із «ОК» після запису — там ідеться про те, що вже лягло в пакет.

Відмова лишилась там, де виправляти нема чого: поля немає, блок не читається,
або суперечність є, а полагодити її нічим. Вигадувати значення в таких
випадках було б гірше за чесне «не можу».

⚠️ Це **прямий запис** у пакет, без планів і без еталона. Прилад виправляє
помилки введення, але не вирішує за вас, які числа правильні:

За правильність самих чисел відповідаєте ви. Перед правками зробіть копію на
вкладці «Ремонт».

> ⚑ **Дати живуть зміщеннями від дати виготовлення.** Тому посунути її й не
> зачепити решту неможливо: похідні дати перераховуються так, щоб лишитись на
> місці. Нуль у похідній даті означає «події не було» — це законний стан;
> дату виготовлення нулем прибрати не можна, без неї решті нема від чого
> рахуватись.

> ⚑ **Чого в Редакторі немає навмисно.** «Заряд за напругою» лишився в
> «Ремонт → Значення»: він нічого не вписує, а **міряє**. Заводський байт
> 0x129 — теж: він лежить у таблиці моделі, однаковій у всіх екземплярів, і
> показань рації правка там не змінює.

### Вкладка «Дані»
- Модель і серійний номер чипа (лазерний ROM DS2438).
- DS2438: напруга, струм (мА), температура, **Залишок (ICA)**,
  **Заряджено (CCA)**, **Розряджено (DCA)** — усе в мА·год і циклах,
  з сирим значенням у дужках.
- Повні hex-дампи DS2438 (64 Б) і DS2433 (512 Б).

### Вкладка «Прошивка»
1. **Зчитування та резервна копія** — зчитати АКБ, завантажити дампи
   DS2433/DS2438 у `.bin`. **Завжди робіть бекап перед записом.**
2. **Запис / відновлення прошивки** — записати `.bin` (512 Б) у DS2433.
   Це і є відновлення повністю стертої АКБ: запис **завідомо справного**
   дампа тієї ж моделі.
3. **Запис DS2438** — записати `.bin` (64 Б) у монітор.
4. **♻️ Скидання для рекалібрування** — обнуляє лічильники заряду/розряду
   (CCA/DCA/таймер) і знос (ємність→100 %), перераховує суми, пише в чіп.
   Далі калібруйте АКБ на **оригінальному** IMPRES-ЗП.
5. **🔋 Змінити залишкову ємність (мА·год)** — рівень заряду записується в
   регістр ICA монітора DS2438 (`ICA = мА·год ÷ (0.4882/Rsense)`). На головному
   екрані пристрою та в огляді (веб/USB) заряд показано в **мА·год** (відсоток —
   дрібним рядком). Окремо є **🔧 знос/здоров'я %** (0–100 %, запис 0x17 + сума).
6. **🛠 Ремонт цілісності** — перерахунок контрольної суми заголовка
   (Σ≡0x41), сум відомих записів (Σ≡0x5A) і відновлення дзеркала калібрування
   з уцілілого DS2438 (`DS2438[24:50]→DS2433[1:27]`). Статус показано:
   заголовок ✅/❌, дзеркало ✅/❌.
7. **🧹 Очистити (крім критичних даних)** — стирає ВСІ дані використання/
   історії/статистики (лічильники, знос, таблиці 0x16), зберігаючи модель,
   серійний/ID, криву калібрування, блок автентифікації та дзеркало
   DS2438↔DS2433. Контрольні суми перераховуються. Доступно і з меню дисплея
   («OK» → група ЛІЧИЛЬНИКИ → «Очистка історії»).
8. **🔥 ПОВНЕ стирання DS2433 (крайній випадок)** — стирає ВЕСЬ чіп DS2433 у
   0xFF, разом з моделлю/серійним/ID/калібруванням. Після цього АКБ не працює,
   доки не запишете еталонний дамп тієї ж моделі. DS2438 не чіпається (з нього
   потім відновиться калібрування). Веб/USB (небезпечна зона) і меню дисплея.
9. **✏️ Редактор байтів (експертний)** — правка будь-яких байт DS2433/DS2438
   прямо в браузері. «Автовиправлення» перераховує суму заголовка DS2433 і
   тримає дзеркало узгодженим. Кнопка «Завантажити» підтягує поточний дамп.

> Вкладку «Прошивка» згруповано: **Резервна копія** → **Обслуговування**
> (безпечно для ідентичності) → **Небезпечна зона** (незворотні операції).
> Спочатку резервна копія; незворотні дії — зі свідомим підтвердженням.

### Операція з пакетом: одне вікно, три режими

Заряд, розряд і пробудження ніколи не йдуть разом — прошивка їх взаємно блокує.
Тому в клієнтах це **одне вікно**, а режим обирається **випадаючим списком**.

Поки операція йде, режим задає сам прилад і список замкнений: показувати
уставки заряду під час розряду означало б обіцяти керування, якого немає. Щойно
операція скінчилась, список знову вільний — навіть якщо підсумок ще на екрані.

Підсумок завершеної операції прибирається кнопкою **«✓ ЗАКРИТИ ПІДСУМОК»** (на
самому пристрої — будь-якою кнопкою). Доти вікно показує саме той режим, який
щойно працював.

### Вкладка «🧙 Майстер»
Автоматичне відновлення: **аналіз** стану → перелік **проблем** → **план** дій
→ покрокове **виконання**. Багатоетапні сценарії з IMPRES-ЗП продовжуються після
повернення АКБ (стан — у журналі ESP32 за серійником). Панель **🗂 Збережені
відновлення** показує заплановані дії й дозволяє їх видалити.

Кожна знайдена проблема або має крок у плані, або **вголос пояснює, чому кроку
немає** — наприклад, правильної дати виготовлення не знає ніхто, крім власника,
і підставити «правдоподібну» означало б вигадати її й закріпити в чипі.

**«Полагодити лічильник розряду»** — крок, який Майстер додає сам, коли в
моніторі DS2438 розряду записано в рази більше, ніж заряду. Значення береться
**з історії того самого пакета**: блок `NONSMART` у DS2433 тримає обидва
лічильники в тих самих одиницях, і зазвичай іде поруч із монітором, трохи
позаду. Пишуться **рівно два байти** — сам лічильник розряду, більше нічого.

Крок **не** з'явиться, якщо: історія не читається; заряд в історії нульовий
(станція цього пакета ще не бачила); історія сама собі суперечить; лічильник
заряду в моніторі теж побитий (порівнювати нема з чим); історія **попереду**
монітора — так виглядає свідомо обнулений або замінений DS2438, і скасовувати
чуже рішення пристрій не буде; або монітор насправді справний.

### Оформлення
Веб-інтерфейс — військовий стиль (хакі/олива/«краповий») з емблемою НГУ. На
кольоровому дисплеї — плавна заставка, плавучий градієнт заповнення батареї й
червоний «світлофільтр» при помилці.

---

## 7. Керування на пристрої (кнопки)

**Кільце показань** (гортається «‹»/«›»): 1. Головна (заряд + статус)
2. Модель / серійний 3. Дані батареї (DS2438) 4. Стан АКБ (ємність/знос/цикли).

**Меню** — усе інше. Відкривається «OK» із будь-якої сторінки показань і являє
собою **список із групами**:

```
  < Показання          <- вихід із меню
РЕМОНТ        Заміна елем. / Глибока чистка / Ремонт цілісн.
ЖИВЛЕННЯ      Заряд ШІМ 100% / Ціль заряду / Розряд 7.20 В / Ціль розряду / Пробудження
ЛІЧИЛЬНИКИ    Заряд з напруги / Скид. лічильн. / Очистка історії
МОДЕЛЬ        Модель <X>  (по одній на вшитий шаблон)
НОВИЙ ЧІП     Новий <X>   (незворотнє)
ДАНІ          Дамп DS2438 / Дамп DS2433
СИСТЕМА       Майстер віднов. / Перезавантаж.
НЕБЕЗПЕЧНО    СТЕРТИ 2433 / СТЕРТИ 2438
```

**Режим 3 кнопок** (`MENU_BTN3_PIN` у `settings.h`):

| | Показання | Меню | Дампи / Майстер |
|---|---|---|---|
| «›» коротко | наступна сторінка | наступний пункт | інший дамп |
| «›» довго | — | **наступна група** | — |
| «‹» коротко | попередня сторінка | попередній пункт | назад у меню |
| «‹» довго | на головну | **попередня група** | до показань |
| «OK» коротко | **відкрити меню** | відкрити сторінку / перемкнути ціль | Майстер: аналіз |
| «OK» довго (0.8…5 с) | зчитати АКБ | **ВИКОНАТИ** операцію | Майстер: наступний крок |
| «OK» 5 с | **перемкнути заряд** (див. нижче) | те саме | те саме |

**Режим 2 кнопок** (третю не задано): те саме правило, лише рух назад — довгим
натисканням. «›» коротко — наступна сторінка/пункт, «›» довго — зчитати АКБ (у
меню — наступна група); «‹» коротко — відкрити меню / обрати пункт, «‹» довго —
виконати операцію.

> ⚑ **Довго тримати треба лише там, де є наслідок.** Операції із записом і
> незворотні стирання — тільки утриманням «OK». А «Ціль заряду» і «Ціль
> розряду» нічого не пишуть і перемикаються **коротким** натисканням: тримати
> кнопку заради зміни числа не треба.

> ⚑ **«Довге OK» спрацьовує, коли ви ВІДПУСКАЄТЕ кнопку, а не на 0.8 с.** На
> тій самій кнопці висить ще й вимикач заряду (утримання 5 с, див. нижче), і
> якби операція виконувалась на порозі, дорога до вимикача проходила б через
> уже зроблене скидання. Щоб не тримати наосліп, екран на кожній сходинці
> пише, що буде, якщо відпустити зараз. Виняток — екрани заряду й розряду: там
> утримання це **аварійна зупинка**, і вона працює на порозі, як і працювала.

> ⚑ **Довгий список — стрибками по групах.** Пунктів разом із шаблонами 36; до
> найдальшого — не більше 13 натискань, і в обидва боки. Назва поточної групи
> й номер пункту («ЖИВЛЕННЯ  5/36») стоять у шапці.

### Пристрій без заряду: як вимкнути й увімкнути

Частину приладів зібрано **без силової частини** — ключа, дроселя й шунта в них
немає, а піни `CHARGE_*` нікуди не підключені. На такому екземплярі пристрій
скаржився б на несправний блок живлення, якого в ньому не передбачено: на
непід'єднаному вході АЦП ловить наводку.

**Тримайте «OK» п'ять секунд** («‹» у режимі двох кнопок) — і відпустіть.
Заряд вимкнеться; те саме утримання вмикає його назад.

Що ви побачите, поки тримаєте:

| Тримаєте | На екрані | Якщо відпустити |
|---|---|---|
| до 0.8 с | — | звичайне коротке натискання |
| від 0.8 с | «відпустіть = ПУСК» (там, де довге натискання щось виконує) | звичайна дія цього пункту |
| від 5 с | **«відпустіть = ЗАРЯД ВИМК»** (або **УВІМК**) | заряд перемкнеться |

Рішення ухвалюється **на відпусканні**, а не на порозі, — і це головне в
жесті. Інакше дорога до вимикача проходила б через уже виконану операцію: на
0.8 с зробилось би скидання, і аж потім, на 5 с, перемкнувся б заряд.

⚠️ **На екранах заряду й розряду жесту немає.** Поки операція йде, утримання
будь-якої кнопки — це **аварійна зупинка**, і вона мусить спрацювати на
порозі, а не тоді, коли ви приберете палець.

Стан зберігається (`/device.cfg`) і переживає перезавантаження: це комплектація
приладу, а не настройка на один сеанс.

Що зникає разом із зарядом:

| | Вимкнено |
|---|---|
| меню пристрою | група **ЗАРЯД** зникає цілком, разом із заголовком |
| контроль живлення | нічого не міряємо: ні «!» у шапці, ні сторінки помилки, ні коду на світлодіоді |
| веб / USB / `.exe` | старт заряду, пробудження, ручні уставки й профіль відхиляються з поясненням «ця версія пристрою — без заряду» |

Пояснення показується **вікном із кнопкою «ОК»**, а не спливаючою підказкою:
воно каже, чому зникла ціла група дій, і зникати саме через три секунди не
має права. Вікно з'являється, коли ви відкриваєте клієнт на такому приладі
(один раз) і щоразу, коли якась дія відхиляється цією причиною. Закривається
кнопкою **«ОК»**, клавішею **Esc** або кліком повз вікно.

Плашка з тим самим текстом лишається в картці заряду — вона тримає відповідь
на видноті після того, як вікно закрили. **Натисніть на плашку**, щоб відкрити
вікно знову.

Що **лишається робочим**: усе інше — читання й запис пам'яті, Майстер, розряд
навантаженням, скидання лічильників і **«Заряд з напруги»** (він нічого не
заряджає, а вписує в паливомір відсоток, порахований із напруги на клемах, — і
на приладі без силової частини це якраз один із небагатьох способів привести
паливомір до тями).

Підказка керування — під списком, і підлаштовується під режим (2 чи 3 кнопки).

---

## 8. Індикація: світлодіоди, звук, підсвітка кнопок (`leds.h`, `buzzer.h`)

| Стан | Сигнал |
|---|---|
| Очікування | короткий зелений пульс раз на 3 с |
| Читання чипа | зелений мигає ~3 Гц |
| Запис у чип | зелений/червоний **поперемінно** (не відключати!) |
| Успіх | зелений горить ~1.2 с |
| Помилка | 4 швидких червоних мигання (+ червоний фільтр екрана) |
| **Несправність живлення +14 В** | **два червоних спалахи, пауза — і так постійно** |
| Фатальний збій старту | постійний червоний |

Індикація неблокуюча — не гальмує кнопки і веб.

### Якщо читання пакета не працює («нема чіпа»)

`GPIO12` — це не лише підтяжка 1-Wire, а й **enable самого акумулятора**: доки
він не піднятий, пакет мовчить, і читання не вдасться, хай яким справним буде
все інше.

> 🔴 **Якщо читання зламалось саме ПІСЛЯ переробки заряду**, перевірте два
> місця — переробка зачепила обидва:
> 1. **GPIO32** — колишній `CHARGE_PIN` (enable силового каскаду). Переробка
>    його прибрала, і пін лишився висіти входом, хоча раніше його опускали в
>    LOW найпершим рядком `setup()`. Якщо доріжка на платі ціла, на вході
>    чужої схеми з'явився невизначений рівень. Прошивка знову утримує його в
>    LOW (`CHARGE_LEGACY_EN_PIN` у `settings.h`).
> 2. **Шунт 0.5 Ом у мінусовому проводі** — він розділив «мінус» пакета й
>    землю ESP32. Чипи DS2433/DS2438 всередині пакета відлічують свою землю
>    від мінусової клеми, а підтяжка й лінія даних — від землі ESP32. Якщо
>    спільної землі не лишилось, шина мовчить так само, як із порожнім
>    роз'ємом. Перевірка нижче це називає прямо.

> 🔴 **РЕЗИСТОР 2.2…4.7 кОм З ЛІНІЇ ДАНИХ (GPIO13) НА 3.3 В ОБОВ'ЯЗКОВИЙ.**
> Внутрішня підтяжка ESP32 (~45 кОм) його **не замінює**, і це не питання
> запасу: DS2433 і DS2438 живляться **паразитно** — струм на власну роботу
> вони беруть із тієї самої лінії даних у паузах між бітами. 45 кОм від 3.3 В
> дають десятки мікроамперів, чипу не вистачає навіть утримати внутрішнє
> живлення, і він не відповідає взагалі. Симптом — «акумулятор не
> зчитується» при цілком справному коді.
>
> Внутрішня підтяжка все одно ввімкнена (`DS_INTERNAL_PULLUP`) як другорядний
> запобіжник: вона рятує від «лінія висить у повітрі» в паузах між операціями.
> Тут є окрема пастка — її **знімає будь-який** `pinMode(pin, INPUT)` (в
> arduino-esp32 `INPUT` = 0x01 не містить біта `PULLUP` = 0x04), а конструктор
> бібліотеки OneWire робить рівно це й більше не відновлює. Тому прошивка
> вмикає її явно після конструктора (`dsIdle()`).

Прошивка тепер перевіряє цю лінію сама — піднімає, читає рівень назад, опускає,
читає ще раз (на ESP32 вхідний буфер вихідного піна лишається увімкненим, тож
видно **реальний** стан виводу, а не те, що записали). Результат у журналі при
старті:

```
1-Wire: пін даних 13, enable/підтяжка 12
  керуючий пін: лінія enable/підтяжки справна
  лінія даних: лінія даних керується підтяжкою правильно (піднімає: внутрішня)
```

У дужках — **хто саме** підняв лінію: керуючий пін, внутрішня підтяжка чи
обидва. На платі без зовнішніх резисторів там має бути «внутрішня»; якщо
написано «ніхто» — лінія притиснута до землі або немає спільної землі з
пакетом.

Два рядки, і вони про різне. **Керуючий пін** — чи піднімається сам GPIO12.
**Лінія даних** — чи піднімається від нього GPIO13, тобто чи працює підтяжка
насправді. Саме другий рядок відповідає на скаргу «підтяжки немає»: керуючий
пін може бути ідеально справним, а підтяжка не доходити.

Якщо лінія несправна, замість «ПОМИЛКА: нема чіпа» на екрані з'явиться
**«ПОМИЛКА: підтяжка (GPIO12)»**, а в журналі — що саме: не піднімається
(коротке на землю, надто низькоомна підтяжка вниз, пробитий ключ) чи не
опускається (коротке на живлення). Це важлива різниця: «нема чіпа» звинувачує
пакет, а причина може бути у власній обв'язці.

> ⚠️ **GPIO12 — strapping-пін (MTDI).** Рівнем на ньому **в момент скидання**
> ESP32 вибирає напругу живлення флеш-пам'яті: HIGH означає «флеш 1.8 В», а на
> більшості модулів вона 3.3 В — і плата просто не завантажиться. Тому:
> * **жодної зовнішньої підтяжки вгору** на самому GPIO12;
> * підтяжка вниз припустима й навіть корисна, але не занадто низькоомна —
>   пін мусить лишатись здатним підняти лінію;
> * якщо обв'язку доведеться переробляти, вільний **GPIO32** підходить краще:
>   повноцінний вхід/вихід без strapping-обмежень.

### Оповіщення про несправне живлення

Без правильного блока живлення заряд не піде взагалі, тому помилка показується
**одночасно на чотирьох поверхнях**, і скрізь із блиманням:

| Де | Як виглядає |
|---|---|
| **екран пристрою** | сторінка помилки з'являється **сама**: **блимаюча червона плашка** з нерухомим текстом усередині — «НЕМАЄ ЖИВЛЕННЯ» / «НАПРУГА ЗАНИЖЕНА» / «НАПРУГА ЗАВИЩЕНА», під ним пояснення, виміряна напруга й **потрібний номінал** («треба 14 В», допуск у дужках) |
| **світлодіод** | постійний код: два червоних спалахи, пауза |
| **веб-інтерфейс** | блимаюча червона смуга вгорі **будь-якої** вкладки |
| **USB-клієнт** (HTML і Python) | така сама блимаюча смуга над вкладками |

Сторінка на екрані **знімається будь-якою кнопкою** — і це навмисно: пристрій
уміє не лише заряджати, читати й правити пам'ять пакета можна взагалі без блока
живлення. Якби сторінка трималась намертво, відсутній блок робив би непридатним
увесь прилад. Після зняття лишаються «`!`» у шапці й код на світлодіоді, а якщо
несправність зміниться на іншу (наприклад, «занижено» → «немає») — сторінка
з'явиться знову.

Блимає **плашка**, а не текст: слова стоять нерухомо й читаються в обидві
фази. Сам рух потрібен — статичну червону смугу за хвилину перестаєш помічати,
а рухома лишається видимою боковим зором. Період — `DISPLAY_PSU_BLINK_MS`
(типово 500 мс). У браузері блимання вимикається системним налаштуванням
«зменшити рух» — смуга з текстом при цьому лишається.

> ⚡ **Код несправності живлення** (`LED_FAULT`) відрізняється від звичайної
> помилки навмисно: «помилка» — це одноразовий сигнал, який минає за 1.6 с і
> означає невдалу операцію з чипом, а два спалахи з паузою тримаються
> **постійно**, доки блок живлення не в порядку. Сплутати їх означало б
> прийняти відсутній блок живлення за невдале читання пам'яті.
> Розшифровка — «`!`» перед номером сторінки на екрані (видно з будь-якої
> сторінки) і повний текст із числами на сторінці заряду, у вебі та в
> USB-клієнті. Пороги: немає `< 6 В`, занижено `< 12.5 В`, завищено `> 16 В`.

> 🔊 **ЗВУК УВІМКНЕНО** — `BUZZER_DAC_PIN 25` у `settings.h`. Раніше тут
> стояло «вимкнено, пін віддано керованому заряду»: друга половина була
> неправдою — ШІМ заряду сидить на GPIO26, а 25-й лежав у резерві. Щоб вимкнути —
> закоментуйте рядок: `buzzer.h` має «тиху» гілку, налаштування й перелік
> сигналів усе одно доступні в API.

**🔊 Звук** (`BUZZER_DAC_PIN`, GPIO 25, пасивний п'єзо через розв'язувальний
конденсатор): **клац** при перегортанні меню + **м'які мелодійні** сигнали
операцій (початок — E5→A5, успіх — G5→B5→D6, помилка — A4→E4). Кожна нота з
огинаючою, гучність — `BUZZER_VOLUME` 0..255 (типово 150). Характер звуку
правиться на ходу (веб/USB → «Ремонт» → «🔊 Налаштування звуку») і живе в
SPIFFS. ⚠️ ЦАП — лише GPIO 25/26; динамік — тільки через підсилювач.

**💡 Підсвітка кнопок** (`BTN_LED_PIN`, необов'язково): рівне світіння у спокої,
блимання під час читання/запису/помилки — як індикатор роботи. `BTN_LED_ACTIVE_LOW`
— для модулів із загальним анодом.

### 🧠 Два профілі, ручні уставки й «≈ лишилось»

**Перемикач профілю** є і в заряду, і в розряду — у вебі, на USB-сторінці, в
`.exe` і в меню пристрою (група «ЗАРЯД» / «РОЗРЯД»).

| | **Заводський** | **Розумний (CC/CV 2S)** |
|---|---|---|
| звідки струм | таблиця за відсотком заряду | **паспортна ємність пакета** з дампа монітора |
| струм заряду | однаковий для будь-якого пакета | 0.5C (2500 мА·год → 1000 мА, 1300 → 650 мА) |
| нижче 6.00 В | той самий стартовий струм | **передзаряд** 0.1C |
| кінець заряду | за відсотком | **за струмом**: ≤ 0.05C п'ять опитувань поспіль |
| струм розряду | лінійка за напругою | 0.2C від паспортної ємності |

> ⚑ **Розумний профіль не зупиняється, коли напруга торкнулась цілі.** Це не
> кінець заряду літію, а початок його останньої фази: у пакеті тоді ~70…80 %
> ємності. Останні 250 мВ проходять на постійній напрузі — уставка струму
> лінійно спадає до порога завершення. Тому розумний заряд **довший** за
> заводський, але й ємності віддає більше: на моделі пакета 2500 мА·год —
> 2.5 год і 2175 мА·год.

> ⚑ **Розумному профілю потрібна паспортна ємність.** Вона береться з дампа
> монітора. Якщо пакет не читається, ємності немає — профіль відкотиться до
> нижньої межі струму. Для нечитаних пакетів спершу **пробудження**.

**Ручні уставки** — струм заряду, ціль у вольтах, струм розряду. Порожнє поле
або кнопка «Авто» = рішення ухвалює профіль. Ціль у вольтах потрібна там, де
відсотки не годяться: **7.40 В** — напруга зберігання (3.70 В на банку), і
ремонтні сценарії, де треба рівно задане число.

> ⚑ **Ручний струм не «сильніше за автомат».** Наприкінці заряду (і розряду)
> береться **менше** з двох — ручного й автоматичного. Підняти струм там, де
> профіль його прибирає, не вийде: це не обмеження інтерфейсу, а захист пакета.

**«Лишилось ≈»** рахує сам пристрій і віддає всім клієнтам однакове число.
**Прочерк означає «оцінити не можна»**, а не «зараз закінчиться»: так буває на
самому початку (струм ще не встоявся), на температурній паузі й тоді, коли
оцінка виходить більшою за стелю часу сеансу. Вигаданого числа тут не буде
навмисно.

**На холоді заряд сповільнюється сам.** Нижче +10 °C розумний профіль зменшує
струм удвічі, нижче 0 °C — **ставить на паузу** (на екрані «ПАУЗА»). Це не
несправність: холодний літій приймає заряд гірше, і повний струм його псує.

### Панель процесу — однакова для заряду, розряду й пробудження

І на дисплеї, і у вебі, і на USB-сторінці, і в `.exe` три операції показані
**одним каркасом**: пульсуючий індикатор стану, велика напруга, смуга
прогресу, крива напруги **за весь сеанс**, коридор уставки струму з рискою,
шпаруватість ключа й вісім плиток у незмінному порядку:

| 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|
| мА·год | струм | потужність | фаза | лишилось ≈ | лічильник чипа | температура | паливомір |

**У пробудженні три останні плитки міняють підпис**, а не зникають: DS2438 у
цьому режимі мовчить, тож замість лічильника, температури й паливоміра стоять
«проб монітора», прочерк і «межа ШІМ». Плитки не переставляються навмисно —
на панель дивляться годинами, і сітка, що перебудовується, змушувала б щоразу
шукати величину заново.

**Як читати коридор уставки.** Заливка — струм, який іде зараз; риска —
уставка; світла рамка — допустимий коридор навколо неї. Поки заливка в межах
рамки, регулятор тримає уставку. Виліз за рамку під час роботи — заливка стає
червоною: це або ключ поза насиченням, або уставку щойно змінили й контур ще
не встиг.

**Крива напруги** збирається за весь сеанс і не «з'їдає» початок: коли точок
забагато, ряд проріджується вдвічі. Пунктир — ціль. У заряді по кривій видно
вихід на плато й початок фази CV; у розряді — «полицю» наприкінці, коли банки
віддають останнє.

### Якщо заряд зупиняється сам

На екрані (і в усіх клієнтах) з'являється текст причини. Найчастіші:

| Повідомлення | Що це насправді | Що робити |
|---|---|---|
| **«АВАРІЯ: пакет від'єднано — на клемі живлення»** | напруга на клемі вийшла за 9.3 В, тобто там стоїть живлення, а не пакет. 6-елементний IMPRES стільки не має: повний — 8.25 В | перевірте контакт клем і пружин та стан лінії enable (GPIO12); можливо, спрацював внутрішній захист пакета |
| **«АВАРІЯ: ключ не тягне — стеля ШІМ без струму»** | шпаруватість у стелі, а струму немає: силовий ключ працює поза насиченням | замініть 1 кОм на **120 Ом** і 20 кОм на **390 Ом** (див. README, «дві заміни перед першим зарядом»); перевірте дросель і пайку |
| **«АВАРІЯ: живлення +14 В поза допуском»** | блок живлення не той або просів | див. розділ вище; допуск 12.5…16 В |
| **«АВАРІЯ: напруга вище межі»** | справжня перенапруга: пакет піднявся вище 8.35 В | зупиніть заряд, перевірте пакет |
| **У Майстрі порожній список моделей** (або напис «(список не завантажено — натисніть 🔄)») | список еталонів приходить командою `TEMPLATES` при під'єднанні, і та одна відповідь не дійшла — шина була зайнята або прилад ще не договорив стартовий звіт | натисніть **🔄** просто біля списку; клієнт запитає його знову й НЕ загубить те, що вже мав. Якщо натомість написано «(у прошивці немає шаблонів)» — повтор не допоможе: у зібраній прошивці справді порожній `templates.h` |

> 🔍 **У журналі по USB кожен рядок заряду містить два виміри напруги:**
> `charge: 8130 mV (чип 8127 mV) …`. Перше — зовнішній подільник на клемі,
> друге — власний монітор DS2438 **усередині** пакета. Поки числа поряд, коло
> ціле. Якщо перше пішло в 9900, а друге лишилось коло 8000 — пакета в колі
> немає, і саме це відсічка й ловить.
>
> Число **9900 мВ ніколи не є виміром**: це підпис відліку АЦП 4095
> (`4095 × 3300/4095 × 30000/10000`). Подільник 20 к/10 к міряє точно лише до
> 9.3 В — вище показання «залипають».

---

### 🩺 Пакет заблокований — примусове пробудження

Режим лікує **дві різні ознаки**, і працює в них по-різному. Спільне те, що в
обох випадках заряд не починається, а причина — заблокований контролер захисту
всередині пакета.

| | що видно | чого чекає режим |
|---|---|---|
| **А. пакет мовчить** | ні DS2438, ні DS2433 не читаються; заряд каже «DS2438 не читається — заряд наосліп заборонено» | доки монітор **заговорить** |
| **Б. пакет читається, а клема мертва** | монітор відповідає, але заряд каже «на клемі *N* мВ, а монітор бачить *M* мВ» | доки на клемі **з'явиться напруга** |

**Чому випадок Б взагалі можливий.** Захист розмикає **силовий** ключ, а
1-Wire сидить **до нього**: монітор говорить, поки клеми розімкнуті. Раніше це
був глухий кут — заряд відмовляв через нуль на клемі, пробудження відмовляло
через те, що пакет читається. Тепер відхиляють лише пакет, який **і читається,
і має напругу на клемі**: там доступний звичайний заряд, і обходити його немає
підстав.

> ⚑ **У випадку Б режим БЕЗПЕЧНІШИЙ.** Монітор говорить — отже температура є, і
> режим за нею стежить (зупиниться на 45 °C). У випадку А датчика немає взагалі,
> і весь захист тримається на межах енергії та часу.

**Ознака випадку А.** Замінили банки — і пристрій не бачить ні DS2438, ні
DS2433, ніби пакета в роз'ємі немає. Спроба запустити звичайний заряд
відхиляється словами «DS2438 не читається — заряд наосліп заборонено».

**Причина.** Пам'ять, найімовірніше, ціла. Заблокований **контролер захисту
всередині пакета**: глибокий розряд перед заміною або звичайне знеструмлення на
час паяння лишають його в стані, з якого він виходить лише коли побачить на
клемах **зовнішню зарядну напругу**. Поки він мовчить, він тримає клеми
розімкненими — а обидва чипи живляться саме звідти.

Виходить замкнене коло: щоб зарядити, треба прочитати; щоб прочитати, треба
подати напругу. Розриває його окремий режим.

**Що зробити.**

1. Спершу **виключіть просте**: підтяжка 2.2…4.7 кОм із лінії даних на 3.3 В,
   сигнал enable (GPIO12), контакт клем, спільна земля через шунт (див.
   «Якщо читання пакета не працює» вище). Пробудження не лікує обірваний дріт.
2. Під'єднайте **блок живлення +14 В** — без нього режим не запуститься.
3. Запустіть:
   - **веб** — вкладка «Заряд», блок «🩺 Пакет не читається після заміни
     елементів», кнопка «ПРИМУСОВО РОЗБУДИТИ ПАКЕТ»;
   - **USB** — кнопка в клієнті або команда `CHARGE WAKE`;
   - **на пристрої** — «OK» → група ЖИВЛЕННЯ → «Пробудження», довге «OK».
4. Дивіться на панель: вона перемикається в режим пробудження й показує час,
   віддані мА·год і кількість проб. Підпис поруч із числом проб каже, **чого
   саме чекає** цей сеанс: «монітор мовчить» (випадок А) або «клема ще мертва»
   (випадок Б). Щойно сталось те, чого чекали, — режим **сам зупиняється** з
   написом «пакет ожив».

**Скільки чекати.** Не більше 2 хвилин — далі режим зупиниться сам. Текст
невдачі залежить від того, чого чекали:

* **«монітор так і мовчить»** (випадок А) — причина не в блокуванні
  контролера: повертайтесь до пункту 1.
* **«клема так і лишилась мертвою»** (випадок Б) — причин рівно дві: захист не
  відпускає, або **обірвано лінію `CHARGE_VSENSE_PIN`**. Відрізнити їх
  вимірюванням неможливо (картина однакова), тому перевірте саму лінію
  мультиметром: на клемі пакета напруга є, а на подільнику нуль — значить
  обрив у ньому.

**Що робити далі, якщо пакет ожив.** Звичайним шляхом: перечитати пакет →
«🔧 Ремонт після заміни елементів» → повний цикл на IMPRES-ЗП.

**Чому цьому можна довіряти.** У випадку А режим працює без контролю
температури — монітор же мовчить, — і саме тому жорстко обмежений: не більше **8 мА·год** відданої
ємності й **120 с** часу. Це менше, ніж пакет втрачає на власний саморозряд за
годину; нагріти його такою енергією неможливо. Напругу на клемах обмежує стеля
шпаруватості (розрахована під 8.00 В = 4.0 В на банку) — вона діє навіть тоді,
коли вимір струму відмовив. Пікова відсічка струму, контроль живлення й
апаратний сторож лишаються чинними.

**Коли режим відмовиться — і це правильно:**

| Повідомлення | Що це означає |
|---|---|
| «Пакет читається **І** на клемі є напруга» | тут доступний звичайний заряд, і він безпечніший — користуйтесь ним. Саму лише читаність режим більше не вважає підставою для відмови |
| «На клемі вже є напруга не нижча за напругу пробудження» | контролер не заблокований. Причину мовчання шукайте в лінії даних і підтяжці |
| «На клемі напруга живлення, хоча ключ закритий» | силовий ключ пробитий. Спершу лагодьте залізо |
| «На закритому ключі шунт бачить струм» | те саме — пробитий ключ |
| «Живлення +14 В поза допуском» | під'єднайте справний блок |

> 🔍 **«АВАРІЯ: піковий струм вище межі» під час пробудження — найімовірніше
> УСПІХ, а не поломка.** У штатному заряді стрибок струму означає обрив дроселя
> чи пробитий ключ. Тут у нього є друге, ймовірніше пояснення: контролер щойно
> замкнув свій ключ, і на місці розімкненого кола раптом з'явились розряджені
> банки. Струм при цьому дорівнює (8.0 В − напруга банок) / опір кола і з
> **біполярним** ключем (у нього немає RDS(on), тож опір контуру менший)
> законно перевищує відсічку 3 А. Відсічка спрацювала й закрила ключ — це її
> робота. **Перевірте, чи пакет тепер читається**: якщо так, пробудження
> вдалося. Якщо ні — тоді вже дивіться дросель, ключ і контакти.

> ⚠️ Режим **нічого не пише в пам'ять пакета** — він взагалі для випадку, коли
> пам'яті не видно. Не лишайте пристрій без нагляду під час пробудження.

---

### 🖼️ Заставка екрана — завантажується, а не вкомпільовується

Картинка, яку пристрій показує при вмиканні, живе у файловій системі приладу.
Замінити її можна без перепрошивання: **веб → «Заставка екрана» → вибрати
файл → НАДІСЛАТИ**. Перетворення робить браузер, встановлювати нічого не треба
— беріть будь-який PNG/JPEG.

**Найпростіше — просто взяти JPEG.** Пристрій розкодує його сам, і в пам'яті
він займе 10…30 КБ замість 113 КБ.

| | JPEG | сирий `.bin` |
|---|---|---|
| Що вантажити | звичайний `.jpg`/`.jpeg` | підготовлений RGB565 |
| Місце на 240×240 | **10…30 КБ** | 115 208 байтів (~113 КБ) |
| Стеля розміру | 64 КБ | 156 КБ |
| Хто перетворює | ніхто — пристрій читає як є | браузер (автоматично) |
| Куди лягає | `/splash.jpg` | `/splash.bin` |

✅ **Розмір підганяється сам.** Не збігається з екраном — браузер впише
зображення в 240×240 **зі збереженням пропорцій** і збереже назад у JPEG
(якість підбирається так, щоб файл влізу у стелю). Нічого зменшувати вручну не
треба: беріть знімок як є.

На самому пристрої теж є зменшення — запобіжник для файлів, що потрапили в
пам'ять іншим шляхом. Воно кратне 2 (1/2/4/8): така властивість формату JPEG.
Відмова лишається тільки для картинок, ширших за екран **понад увосьмеро**
(для 240 px це 1921 px і більше).

| | |
|---|---|
| Коли з'явиться | при **наступному** вмиканні пристрою |
| Повернути типову | кнопка «ПОВЕРНУТИ ТИПОВУ» |
| Обидва формати одночасно | ні — новий витісняє попередній |

> 🧩 Підтримка JPEG вимагає бібліотеки **TJpg_Decoder** (Bodmer): Arduino IDE →
> Менеджер бібліотек → «TJpg_Decoder». Без неї закоментуйте
> `DISPLAY_SPLASH_JPEG` у `settings.h` — лишиться сирий `.bin`, який не тягне
> жодних залежностей. Збірка без бібліотеки при увімкненому прапорці скаже про
> це прямо, а не впаде десь усередині.

Картинка може бути й **меншою за екран** — її відцентрують. Більшу за екран
пристрій відхилить і скаже про це.

> ⚠️ Файл перевіряється при прийманні тим самим кодом, яким його потім читає
> дисплей. Якщо надіслати не те (наприклад, PNG замість підготовленого `.bin`),
> прилад відповість «не той формат», а стара заставка залишиться недоторканою:
> приймання йде в тимчасовий файл, і бойовий підміняється лише після перевірки.

Без браузера те саме робить `tools/make_color_splash.py logo.png --bin` —
отриманий `splash.bin` завантажується тією ж кнопкою. Стара можливість
вкомпілювати картинку (`--bin` не вказувати, `DISPLAY_SPLASH_CUSTOM`) нікуди не
поділась; завантажений файл її **перекриває**.

---

### 📶 Підключення по Bluetooth

> 🔴 **ЗАРАЗ ВИМКНЕНО.** На залізі з увімкненим Bluetooth пристрій перестав
> під'єднуватись по Wi-Fi: стек Bluedroid забирає десятки кілобайтів купи, а
> Wi-Fi і класичний BT ще й діляться спільним радіо. Wi-Fi тут головний канал,
> тож Bluetooth типово вимкнено. Увімкнути — розкоментувати `BT_ENABLED` у
> `settings.h`; при цьому подивіться рядок «Вільна купа: …» у журналі при
> старті ДО і ПІСЛЯ вмикання.

Пристрій може віддавати **той самий командний протокол**, що й по USB, поверх
Bluetooth SPP. Для комп'ютера це звичайний COM-порт, тож `moto_gui.py`,
`moto_bridge.py` і `client_usb.html` працюють **без змін** — оберіть у списку
портів той, що з'явився після спарювання.

1. У списку Bluetooth знайдіть **`MotoBattery-XXXX`** (чотири цифри — з MAC,
   щоб два прилади поруч не плутались).
2. Спаруйте, PIN за замовчуванням **`1234`** (`BT_PIN` у `settings.h`).
3. У системі з'явиться послідовний порт — оберіть його в клієнті замість USB.

> 🔒 **По радіо читання вільне, а запис вимагає пароля.** По USB перепусткою є
> сам кабель, тож там усе відкрито. По Bluetooth у радіусі дії опиняється
> будь-хто, а серед команд є `WIPE33` — повне стирання пам'яті пакета. Тому
> перед першим записом надішліть `AUTH <пароль>` (у клієнтах це поле пароля).
>
> ⚠️ **Змініть `ADMIN_PASSWORD`** у `settings.h`: стандартний «admin123» по
> радіо майже те саме, що без пароля. Пристрій попереджає про це при старті.

**Що потрібно при збірці:**

| | |
|---|---|
| Схема розділів | **Huge APP (3MB No OTA/1MB SPIFFS)** — Arduino IDE → Інструменти → Partition Scheme |
| Плата | лише звичайний **ESP32**: на S3/C3/C6 класичного Bluetooth немає фізично |
| Не потрібен BT | лишіть `BT_ENABLED` закоментованим (так зараз) — стек навіть не збирається |

Без зміни схеми розділів прошивка зі стеком Bluetooth просто не влізе. Перевірити
це на компіляції неможливо (розділи задаються в IDE), тому пристрій друкує
вільну пам'ять при старті, а `BluetoothSerial` скаже, якщо не запустився.

⚠️ Радіо одне на Wi-Fi і Bluetooth: разом вони працюють, але діляться ефіром і
пам'яттю. Не потрібен Wi-Fi — вимкніть точку доступу, обом стане легше.

---

## 9. Одиниці вимірювання

- **Лічильники заряду — у мА·год:** `мА·год = регістр × 0.4882 / Rsense`.
  Резистор струму: `settings.h → DS2438_RSENSE_OHM` (за замовч. 0.025 Ом) —
  підберіть, щоб ємність сходилась з паспортною (типово 20…40 мОм).
- **Цикли:** 1 цикл = сумарний заряд (розряд), рівний одній повній
  паспортній ємності. Ємність АКБ: `settings.h → BATTERY_RATED_MAH`
  (за замовч. 2500; PMNN4409 — 2500, PMNN4493 — 3000).
- **Заряд, %:** пріоритет ICA (якщо DS2438 рахує струм, IAD=1), інакше
  оцінка за напругою (`BATTERY_EMPTY_MV`…`BATTERY_FULL_MV`).

### Коли показання каже «не знаю» — і чому це важливо

Частина полів АКБ лежить у блоках із контрольними сумами, і блок буває
побитим. У таких місцях пристрій пише словами, а не числом:

| Що бачите | Що це означає |
|---|---|
| **«Циклів не-IMPRES: не читається»** | блок лічильника побитий. Це **не** «жодного разу не заряджали простою ЗП» — таке теж буває, і тоді там стоїть чесний **0** |
| **«Дата виготовлення: ⚠ неправдоподібна (блок DATE побитий)»** | блок не розшифровується ключем цього чипа. Раніше на його місці друкувалось саме те, що вийшло, — приміром «2072-14-22» |
| **«Перше користування: — не обчислити (немає дати виготовлення)»** | воно рахується **від** дати виготовлення, тож без неї не рахується. Це не те саме, що «— (пакет ще не вмикався)» |
| **Майстер: «Лічильник РОЗРЯДУ монітора побитий»** | у DS2438 «взято» у рази більше, ніж залито (буває 26 млн мА·год при 191 циклі заряду). Майстер пропонує крок **«Полагодити лічильник розряду»** — бере значення з історії самого пакета (див. нижче) |

> ⚑ **ЕТАЛОН ТЕЖ НІС ЧУЖИЙ НАРОБІТОК.** Еталон — знімок донорського пакета, і
> разом із моделлю він приносив CCA/DCA донора. Скидання їх обнуляло, еталон
> писав назад, станція переносила в монітор — і це виглядало як «скидання не
> працює». Тепер відновлення завжди лишає наробіток САМОГО пакета (на чистому
> чипі — нулі). Виняток один: **дослівне відновлення (verbatim)** — його зміст
> у тому й полягає, щоб не міняти жодного байта.
>
> ⚑ **ЧОМУ ПІСЛЯ СКИДАННЯ ЛІЧИЛЬНИКИ МОГЛИ ПОВЕРТАТИСЯ.** Наробіток записаний
> **двічі**: у моніторі DS2438 і в самому пакеті (DS2433, блок `NONSMART`).
> Зарядна станція вважає оригіналом **пакет** і після кожного сеансу
> пересинхронізовує з нього монітор. Доти скидання обнуляло тільки монітор —
> і перша ж зарядка повертала старі числа з пакета. Тепер обнуляються **обидва
> боки**; якщо переписати історію в пакеті не вдалось, скидання відмовляється
> цілком і монітор не чіпає.
>
> Саме тому **різні станції не псують дані**: станція нічого не вигадує, вона
> лише повертає в монітор те, що прочитала з пакета. Виняток один —
> **наробіток ETM**: його станція відновлює за власним годинником («скільки
> було + скільки минуло»). Сперечатися з цим марно, тому наробіток правлять
> **ПІСЛЯ** калібрування, а не до нього.

> ⚑ **Прочерк тут — не порожнеча інтерфейсу, а показання.** Число замість
> нього означало б, що пристрій знає те, чого не знає, і саме на такому числі
> будувалося б рішення: скільки циклів дописати, яку дату вписати, що
> синхронізувати. Усі чотири випадки взяті з реальних дампів власника
> (`dumps/20-vymahaie-vidnovlennya/`).

---

## 10. Формат прошивки IMPRES (за аналізом дампів)

- **Контрольна сума заголовка DS2433:** `сума 0x00..0x1F ≡ 0x41`
  (байт 0x1F — контрольний).
- **Контрольна сума TLV-записів:** сума байт запису разом з її контрольним
  байтом `≡ 0x5A` (напр. запис історії ємності 0x17).
- **Дзеркало калібрування:** `DS2438[24:50] == DS2433[1:27]` на кожній
  справжній АКБ. Блок **однаковий для всіх батарей однієї моделі**
  (4488A і 4493A збігаються, 4409A відрізняється).
- **Модель** — в авторитетному записі `0x0B` (`0B 'P' ...`), а також
  дублюється як ASCII у заголовку.

**Прив'язки до серійного номера чипа НЕМАЄ.** Побайтове порівняння двох
батарей однієї моделі показує: усі відмінні байти — лічильники
використання/телеметрія; підпису від лазерного ROM DS2438 у даних немає.
Прошивка прив'язана до **МОДЕЛІ**, тому відновлення можливе з еталона тієї
ж моделі без підбору серійника.

---

## 11. Типові сценарії

**Відновити повністю стерту АКБ (DS2433 = нулі, DS2438 цілий):**
запишіть `.bin` еталонної батареї тієї ж моделі (вкладка «Прошивка» →
«Запис/відновлення»), потім «🛠 Ремонт цілісності» — він синхронізує дзеркало
з уцілілого DS2438 цієї батареї.

**Пошкоджений заголовок/калібрування (рація «не бачить»):**
«🛠 Ремонт цілісності» — перерахує суми і відновить дзеркало.

**Оригінал «невідомий» або не вмикається:** якщо суми/дзеркало в нормі —
проблема не в прошивці, а в елементах (розряджені/мертві) або потрібне
калібрування на IMPRES-ЗП. Спробуйте «♻️ Скидання для рекалібрування».

**Клон з обнуленим DS2438** (напр. PT4409A): рація відхиляє через відсутність
калібрування; повне відновлення потребує еталонного DS2438 тієї ж моделі.

**Змінити показник ємності:** «🔋 Змінити ємність» → введіть % → запис.

**Точкова правка будь-якого байта:** «✏️ Редактор байтів» → «Завантажити» →
правка → «Записати байти» (з увімкненим автовиправленням сум).

> ⚠️ **Автоматична «розлочка» без еталона неможлива** — калібрувальні дані
> унікальні для моделі й не виводяться математично. Перед будь-яким записом
> робіть резервну копію.

---

## 12. Файли проєкту

| Файл | Призначення |
|---|---|
| `SCHEMATIC.html` | **принципова схема** — 5 листів, карта пінів, перелік доопрацювань. Відкривається просто у браузері |
| `motorola-battery-reader-web.ino` | головний скетч (setup/loop) |
| `settings.h` | усі налаштування (піни, дисплей, звук, підсвітка кнопок, анімація) |
| `battery_reader.h/.cpp` | протокол 1-Wire, читання/запис DS2433/DS2438 |
| `display.h` | монохромні дисплеї: меню, декодування, контрольні суми |
| `display_color.h` | кольорові ST7789: меню, заставка, градієнт, червоний фільтр помилки |
| `web_server.h` | веб-сервер, API, ремонт/редактор/скидання/новий АКБ |
| `recovery.h` | двигун Майстра відновлення + база правил + журнали за серійником |
| `templates.h` | вшиті genuine-еталони моделей |
| `leds.h` | неблокуюча індикація (світлодіоди + підсвітка кнопок) |
| `buzzer.h` | звуковий буззер (необов'язковий) |
| `serial_api.h` | командний протокол по USB-Serial (для Windows-клієнта) |
| `client_usb.html` | Windows/Web-клієнт по USB (Web Serial, Chrome/Edge) |
| `index.html` | веб-інтерфейс (у `data/` для SPIFFS) |
| `page_index.h` | веб-сторінка, вшита в прошивку (gzip). Перегенерувати після правки `index.html`: `python3 tools/mk_page_header.py` |
| `data/` | необов'язкова тека для SPIFFS; сторінку туди класти не треба |

---

## 13. Робота по USB (Windows-клієнт)

Пристрій дублює весь функціонал вебу через **USB-кабель** (той самий, яким
прошиваєте). Працює **паралельно** з Wi-Fi.

**Клієнт — `client_usb.html`** (Web Serial):
1. Відкрийте `client_usb.html` у **Chrome** або **Edge**.
   - Якщо сторінка пише, що Web Serial недоступний з `file://`, покладіть файл
     у теку і запустіть `python -m http.server`, відкрийте
     `http://localhost:8000/client_usb.html`.
2. Натисніть **«Підключити»** → оберіть COM-порт ESP32 (той самий, що в
   Arduino IDE). Зачекайте ~1.5 с (ESP32 перезавантажується при відкритті
   порту — це нормально).
3. Далі — той самий інтерфейс, що у вебі: вкладки Огляд / Дані / Прошивка,
   зчитування, дампи, запис, скидання, ремонт, зміна ємності, редактор байтів.

**Протокол** (`serial_api.h`, 115200 бод) — текстові команди, відповідь одним
рядком `#R#{json}`; підходить і для власного клієнта (Python/C#):

| Команда | Дія |
|---|---|
| `PING` | ідентифікація (`ver`, `authed`) |
| `READ` | зчитати обидві мікросхеми |
| `INFO` | усі декодовані поля + дампи (hex) |
| `GET33` / `GET38` | сирий дамп (hex) |
| `WRITE33 <hex>` / `WRITE38 <hex>` | запис дампа (як є) |
| `WRITEFIX33 <hex>` | запис DS2433 + автопочинка суми/дзеркала |
| `RESET` / `REPAIR` / `CLEAN` | скидання / ремонт / очистка |
| `WIPE33` / `WIPE38` | повне стирання DS2433 / DS2438 |
| `RECAL` | підготовка до рекалібрування (рецепт після заміни елементів) |
| `SETCAP <0..100>` / `SETMAH <мА·год>` | ємність % / залишкова ємність |
| `SETCHG <0..100>` | заряд % (ICA); при застряглому паливомірі — за напругою |
| `SETMODEL <NAME>` | ручний запис моделі (3..9 символів `A–Z 0–9`) |
| `SETETM <сек>` | ETM (наробіток) |
| `TEMPLATES` | список вшитих моделей-еталонів |
| `INITBAT <MODEL> <мАг>` | ініціалізувати порожній чип як новий АКБ моделі |
| `RESTORE <MODEL>` | відновити еталон **verbatim** |
| `WIZARD` / `WIZSTEP <idx> [MODEL]` / `WIZRESET` | Майстер: аналіз / крок / скидання |
| `CHARGE DISMISS` / `DISCHARGE DISMISS` | прибрати підсумок завершеної операції (панель повертається в початковий стан) |
| `WIZLIST` / `WIZDEL <serial>` | список / видалення журналів відновлення |
| `AUTH` | *(історична)* завжди «ОК» — пароль прибрано |
| `REBOOT` | перезавантаження ESP32 |

> 🔓 **Пароль прибрано** — і по USB, і у веб-API операції доступні напряму.
> `AUTH` лишено для сумісності (повертає «ОК»).
> Відладкові рядки у Serial не мають префікса `#R#` — клієнт їх ігнорує.
